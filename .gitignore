/target
# Cargo.lock IS committed: this is a workspace with a binary crate
# (tools/cargo-midnight) and the lockfile pins exact dependency versions
# for reproducible builds.
*.swp
*.swo
*~
.DS_Store

# Reference repos are not part of this project's source
/reference-repos
